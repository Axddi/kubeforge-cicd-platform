apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubeforge-green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubeforge
      version: green
  template:
    metadata:
      labels:
        app: kubeforge
        version: green
    spec:
      containers:
      - name: kubeforge-app
        image: nginx:alpine
        ports:
        - containerPort: 8080
